import { DefaultTheme } from 'react-native-paper';

export const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: '#00B14F', // A green color inspired by the logo
    accent: '#FFFFFF',
    background: '#FFFFFF',
    text: '#000000',
  },
};
